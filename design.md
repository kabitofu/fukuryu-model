# 伏流モデル 設計

---

伏流モデルは、局所的な相互作用のみを持つノードが自律的に振る舞うことで、
設計者が明示的に構造を作らなくても知能的な処理が創発することを目指したネットワーク・フレームワーク。
「意味流通自己組織的ネットワーク」とも呼べる：信号が意味を運び、ノードは信号が持つベクトル（意味）を覚えながら経路を形作る。

各ノードは自分の近傍にある情報（にじみ・到着信号・接続相手のエネルギー）だけを見て動作する。
全体の状態を知るノードは一つも存在しない。
それにもかかわらず、クラスタ形成・新陳代謝・クラスタ間接続・学習・記憶分離といった
高次構造が自然に創発することを設計の出発点とする。

設計の着想は脳・社会・経済・宇宙といった多様なスケールに共通する自己組織化の原理に基づいている。

---

## 基本要素

システムは以下の要素で構成される。クラスタ・接続・野などの上位構造は、
これらの相互作用から自然に創発するものであり、設計者が手作りするものではない。

| 要素 | 説明 |
|---|---|
| **ノード** | 処理単位。ベクトル・エネルギー・温度を持つ |
| **信号** | ノード間を伝播するパケット。ベクトルとエネルギーを持つ |
| **エネルギー** | 信号の強さであり、ノードの生存燃料 |
| **温度** | ノードの局所的な活性状態 |
| **にじみ** | ノードが周囲に常時放つ微弱な自己告知 |
| **場** | ノードが存在する空間。積極的に冷やそうとする |
| **クラスタ** | 意味的に近いノードが自然に集まった集合（外部観測概念） |
| **野** | クラスタより大きな領域区分（自然形成） |
| **IN / OUT** | 外界との接続端末 |

---

## 信号

信号はベクトルとエネルギーを持つ。エネルギーとベクトルは一体として動く。
信号は液体のような性質を持つと考えられており、液体が場を流れるイメージに近い。

信号のエネルギー減衰方式は未確定（「決まっていない重要な設計」参照）。

複数の信号が同時に同じノードに届いた場合は重ね合わせ（Superposition）で合成される。

### 信号交差（通過ノードとの相互影響）

信号がノードを通過するとき、信号とノードは互いに少しずつ影響し合う。

```
新しい信号ベクトル   = 信号 × (1-r_signal)  + ノードベクトル × r_signal
新しいノードベクトル = ノード × (1-r_node)   + 信号ベクトル  × r_node
```

r_signal と r_node は別々に設定できる（非対称交差）。
信号エネルギーが強いほど交差の割合が大きくなる方向が有力。

この仕組みにより、たらい回しにされ続ける信号は徐々に「ぼんやりした意味」になり、
最終的にどこかのノードと共鳴して吸収される（「永遠に処理されない信号」問題の自然な解消）。

### 信号衝突の処理方針（未確定）

**案A：相互破壊**（診断用のみ）
異なる信号が衝突して互いのエネルギーを消耗させる。通常の学習ルールとしては不採用。

**案B：並列スルー**（当面の実装方針）
衝突を明示的に処理せず、Superposition（加算）で重ね合わせて並列に流す。
VSAの高次元直交性に依存して自然に分離することを期待する。

**案C：側抑制による分離**（将来的な理想形）
同じ意味の信号が衝突したときは側抑制で1本に絞り込む。
異なる意味の信号が衝突したときは共存させる（並列処理）。
局所的なVSA演算（類似度閾値）のみで自律判定する実装方法は未解決。

---

## ノード

### 保有情報

| 情報 | 説明 |
|---|---|
| VSAベクトル | 意味内容・感覚データ・処理ロールを統合した単一の高次元超密度ベクトル |
| エネルギー | 生存燃料。信号の受け取りで増え、代謝・告知・発火で減る |
| 局所温度（T_local） | 発火の継続で上昇。場が冷やそうとする |

### ベクトルの取り込み（blend）

信号が届くたびに、残エネルギーに応じた割合 r でベクトルを取り込む。

- 飢えているほど（残エネルギー少）→ r 大：外部ベクトルに染まりやすい
- 満たされているほど（残エネルギー多）→ r 小：自分のベクトルを保つ

この割合はさらにACK信号（後述）の強さとも連動する。
ACKが多く返ってきているノードは「今の自分でうまくいっている」ため染まりにくくなる。

### 発火

**通常発火**：保有ベクトルと到着信号ベクトルの類似度（resonance）が閾値を超えたとき。
エネルギーを放出しながら信号を次に送る。T_local が上昇する。発火するほど寿命が伸びる。
発火時のエネルギー消費が激しいため、発火後は発火抑制（不応期）が必要と思われる。

**エラー発火（強制発火）**：残エネルギーが上限を超えたとき。
内側からの圧力でゲートが破れる受動的放出。送り先を選ばない。
エネルギーの一部が送信元へ反跳（返却）される。
新しい意味の偶発的な取り込みや連想の広がりを生む可能性がある一方、連鎖エラー発火のリスクも持つ。

### 物質の三態

ノードは局所温度の高さによって振る舞いが変わる。

| 状態 | 温度 | 振る舞い |
|---|---|---|
| **固体**（低温） | 冷えて固まる | 処理不要。にじみのみ放射。信号が向かわない限り負担にならない |
| **液体**（適温） | 程々 | 転送のみ担う。多少ベクトルを吸い取りながら次に送る（可能性） |
| **気体**（高温） | 熱くなる | 活発にベクトルを取り込み、発火・学習する |

場は積極的に冷やそうとするため、ノードは自然に固体方向へ向かう。
発火が続くときだけ温度が上昇して気体状態を維持できる。

### 温度の生成と場による冷却

エネルギーが消費されるとき（発火・転送・にじみ）に熱が発生し、ノードの局所温度が上昇する。
場は常に積極的に冷やそうとするため、発火が続いているノードだけが高温を維持できる。
温度は位置から機械的に割り当てられるのではなく、各ノードのエネルギー消費の結果として生まれる。
活発な処理領域と静かな記憶領域が自然に分離される。

三態の判定は「温度が一次因」ではなく、実質的には「共鳴の成否」がトリガーである。
「発火するから温度が上がる」という因果の方向を守る。

### 経路の段階的固定化

初期状態ではベクトルが弱く（短い）、共鳴度が低い。
そのため最初の信号は広く薄く全方向に探索する（蛇行）。
繰り返し同じ信号が通ることでベクトルが育ち、共鳴度が上がり、特定の経路が強化されていく。

もしベクトルが最初から完全に成長した状態だと、最初の信号で即座に経路が固定化されてしまい（一瞬で直線）、探索が起きない。
初期の探索フェーズを確保することが健全な学習には不可欠。

### にじみ

ノードは常時、自分のベクトルとエネルギーを近傍に微弱に漏らす。
エネルギーとベクトルは一体なので、にじみはエネルギーを消費する。
にじみの強度はエネルギー残量に応じて調整される。

エネルギーが少ないとき告知を弱めると発見されにくくなりさらに追い詰められる。
このジレンマ自体が自然な生存戦略の動態を生む。

にじみの役割は二つある。

| 役割 | 内容 |
|---|---|
| **存在告知** | 近傍に自分の波長を知らせ、他ノードの移動・接続判断の材料になる |
| **報酬の帰還** | 発火後ににじみが送信元ノードへ逆流し、有効だった経路が強化される |

### ライフサイクル

死んだノードの空きに新しいノードが生まれる。
新生ノードの初期化方針（周囲コピーかランダムか）と配置方針（需要ベースかランダムか）は未確定。

自律的な三役職遷移（外部から管理しない）：
1. **白紙の若手**（外縁・高温）：多様な信号を浴びてベクトルが変化しやすい
2. **特化した職人**（コア・低温）：特定の信号に高く共鳴する。安定して長寿
3. **泥水の経路**（外周・適温）：多様な信号を通した結果、どの信号ともそこそこ共鳴する液体ノード。クラスタ間橋渡しを担う

---

## ノードの動機とエネルギー経済

エネルギーは栄養でもあり毒でもある。この対称的な制約がノードの能動的な振る舞いを生む。

- 発火しないと枯渇して死ぬ → 信号を引き寄せようとする
- エネルギーを持ちすぎると死が近づく → 周囲に送り出そうとする
- 発火するほど寿命が伸びる → 信号の流れの中に自分を置こうとする

### 接続の方向

- ノードは**自分よりエネルギーを持つノード（より富めるもの）に接続しようとする**
- エネルギーが多すぎるノード（クラスタ中心部・IN/OUT 周辺など）は**自分より弱いノードに接続しようとする**
- 接続は有向グラフを形成する。信号は一方向に流れる。双方向になるのは両側から手を伸ばしたとき

ノードはより自分のベクトルに近い「にじみの匂い」を感じたとき、既存の接続を離れてその方向に向かおうとする。
相手ノードからも接続されている場合の扱いは未確定。これがクラスタ間の引き寄せ力になる可能性がある。

### 発火しない信号の転送コスト

類似度が閾値未達の信号を転送するとき、ノードは**自分のエネルギーを消費して**信号を通す。
役に立たない信号が多いノードはエネルギーが枯渇してクラスタから押し出される。
この仕組みにより、意味的に適切な経路が自然に形成され、不要な経路は淘汰される。

### にじみへの引き寄せと誘蛾灯

ノードは**自分のベクトルに近いにじみに引き寄せられる**。
クラスタが活発に発火するほど多くのノードのにじみが重なり合い、外側ほど弱くなるグラデーションができる。
これはクラスタが特定の波長の光を放つ誘蛾灯のように機能することを意味する。
意味的に近いノードほどその光に引き寄せられ、遠いノードには届かないか届いても共鳴しない。
ノードはクラスタを認識しなくても、にじみの勾配を辿ることで自然にまとまりに向かえる。

---

## エネルギー

### ノードのエネルギー収支

| 収入 | 支出 |
|---|---|
| 信号の受け取り | 代謝コスト（存在するだけ） |
| ACK信号の帰還 | 告知コスト（にじみ放射） |
| にじみ帰還（発火時） | 発火時手数料（激しい消費） |
| | 発火しない信号の転送コスト |

### システム全体のエネルギー収支

外部からエネルギーが入る口として IN が確定している。
場からも供給されるかどうかは未確定（「決まっていない重要な設計」参照）。

エネルギーが出ていく出口が必要でないと系全体でエネルギーが発散する。

出口の候補：
- OUT が受け取ったエネルギーの一部を系外へ放出する
- 発火・転送の手数料として消費されたエネルギーが「熱」として散逸する（確定）
- 系のどこかに「仮想捨て先（虚空）」がある（エラー発火が行き場を失ったとき専用の散逸口）

---

## クラスタ

クラスタはノードが内部的に認識する構造ではない。
ノードは「近傍のにじみと信号を見て判断する」だけであり、自分がどのクラスタに属するかを知らない。
クラスタは外から観測したときに「意味的に近いノードが集まっている」と見えるパターンにすぎない。

### クラスタの形成と学習

似た信号が来続けると、クラスタ全体が温まって発火しやすくなり、ベクトルを取り込みやすくなる。
やがて発火して瞬間的に熱くなり、学習が始まる。

### クラスタの新陳代謝

クラスタ中心部ほど熱くなるため、中心付近のベクトルは様々な信号を取り込んで濁りやすい。
濁ったノードは発火しにくくなり、エネルギーを得にくくなるため外縁へ弾き出される。
外縁から生まれたノードが中心へ沈み込む形で内側から外側への動的な新陳代謝が起きる。

### クラスタの圧と引力（外部観測値）

```
クラスタの圧 = Σ残エネルギー / ノード数
クラスタの放射ベクトル = Σ(各ノードのベクトル × 残エネルギー) / Σ残エネルギー
```

これはデバッグ用の外部観測値。ノードはこれを直接参照しない。

---

## ノードの誕生と消滅

発火が少なくなったノードはくっつきやすさが下がり、接続が薄れ、最終的に消える。
逆に信号が来なくなったノードも自然に死に向かう（信号が来ているうちは寿命が延び続ける）。

ノードが消えると空きができ、新しいノードが生まれる。
新生ノードの初期化方針（周囲コピーかランダムか）と配置方針（需要ベースかランダムか）は未確定。

この誕生と消滅のサイクルがネットワーク全体の動的な新陳代謝を担う。

---

## 快・不快と温度管理

外界からの快・不快信号は、ネットワーク全体の温度管理として機能する。

**快＝冷やす**：くっつきやすさを上げて現在のクラスタ構造を安定させる。
成功した経路・パターンを凝固させて定着させる仕組み。

**不快＝温める**：ランダムなノイズを注入し、くっつきやすさを下げてノードを漂流させる。
硬直状態から脱出し、新しい可能性を能動的に探索させる仕組み。

この二つが組み合わさることで、「学習と探索のバランス」が温度という単一の軸で制御される。

---

## クラスタ間の接続

クラスタが成長するにつれてクラスタ同士の間に接続が生まれる。

**案1（内からの押し出し型）**

クラスタ中心で濁ったノードが外縁部に積もり始める。
そのノードは濁ってはいるが、元クラスタ周辺の信号には馴染みがある。
周囲のクラスタで同様に外縁部に積もったノードと徐々に近づいて接続し、橋を形成する。
「似たノードが集まる」という特性はクラスタ間にも継承されるため、時間をかけると似たクラスタ同士が集まる。
→ 大きくなるほど激しく新陳代謝されるため、クラスタの肥大化が自然に抑制される。

**案2（外周からの引き寄せ型）**

クラスタ外周部のノードはそのクラスタに染まりきっていないため、周囲のクラスタにも引き寄せられやすい。
周囲のクラスタの外周部も同様であるため、徐々に相互接続が始まる。
クラスタのベクトルが十分に近い場合はクラスタが統合される。

どちらの案であっても、橋渡しノード（ブリッジ）の共通した特徴：
- どのクラスタとも完全には一致しない「泥水化したベクトル」を持つ
- 信号が通り続ける限り寿命が延びる
- 信号が途絶えると速やかに死ぬ（経路の自動剪定）

---

## ACK信号（逆流確認応答）

下流ノードあるいはOUTが発火・出力を達成したとき、その成功信号が上流に向かって逆流する。
これは信号の正方向とは逆方向に流れる別チャネルとして存在する。

| | にじみ帰還 | ACK信号 |
|---|---|---|
| 発生源 | 発火したノードから | 下流ノード・OUTから |
| 強さ | 弱い（にじみ） | 強い（明示的な信号） |
| 対象 | 近傍全体・減衰して届く | 上流の特定経路 |
| 役割 | 軽い報酬信号 | ルーティング方向の強化 |

ACK信号の強さが上流ノードのルーティング判断に影響する：
複数の出力方向がある場合、ACKが強く返ってくる方向に信号を多く割り振る。
局所的なACK強度の比較だけでグローバルな目標を知らずにルーティングが最適化される。

**フィードバックの受取先**：発火に最も多く貢献したのは「今まさに信号を送ってきたノード」である可能性が高い。
そのため、ACK信号は直前の送信元ノードが最も多く受け取る方向が有力。

ACK信号の未確定点：
- ACKは連鎖的に何ホップ遡るか（1ホップのみか、減衰しながら複数ホップ伝播するか）
- にじみとACKを別チャネルで実装するか、強度差のある同一チャネルにするか

---

## 接続と有向グラフ

### 接続の形成ルール

- ノードは自分のにじみと相手のにじみを照合して接続先を判断する
- 自分のベクトルと近い波長のにじみが来ている方向に接続しようとする
- 接続は隣接リスト（OUTリスト・INリスト）として管理される

### 接続の向きの決定

1. **くっつきやすさの差**：低いノードが手を伸ばし、高いノードが受け取る方向
2. **エネルギー飽和度**：溜めすぎているノードは弱いノードへ向けて手を伸ばす

信号はダイオード的に一方向に流れる。双方向になるのは両側から互いに手を伸ばしたとき。

### 接続の切り替えと移動

ノードはより自分のベクトルに近い「にじみの匂い」を感じたとき、
既存の接続を離れてその方向に向かおうとする。
相手ノードからも接続されている場合の扱いは未確定。これがクラスタ間の引き寄せ力になる可能性がある。

### エッジの物理設計

ノード間の接続（エッジ）は完全な論理構造か物理空間を持つかの中間的な設計が検討されているが、詳細は未確定。

### 経路の形成と淘汰

初期は広く弱く多方向に接続が試みられる（探索フェーズ）。
有効な経路（ACKが多く返ってくる）はくっつきやすさが上がり、太く定着する。
役に立たない信号しか来ない経路は手数料でエネルギーが枯渇し、自然に消える。

---

## 定常波とリフレイン

クラスタ内でノードが互いに発火し合い、信号が経路を循環し続けると「定常波」が生まれる。
これが「今考えていること」の物理的な実体になりうる（ワーキングメモリに相当）。

定常波は：
- ループする信号がホップ減衰で自然に消えるため、外部からの補充がないと消える
- 外部からの新しい信号が来ると既存の定常波と相互作用して更新される
- 定常波の強さ（エネルギー密度）が閾値を超えたとき初めて OUT やハブへ送られる

これにより「ランダムノイズによるスパム送信」が防がれ、意味のある信号だけが出力される。

---

## VSA（Vector Symbolic Architecture）

ベクトルは VSA の高次元超密度ベクトル（HDV）を用いる。高次元であるため、ランダムなベクトル同士はほぼ直交する。

主要演算は二つ。

**結合（Binding）**：XOR 演算。
「A と B を結び付けた概念」を生成する。結果は A にも B にも似ておらず、直交に近い新しいベクトルになる。
キー（A または B）があれば元の概念を取り出せる（Unbinding）。

**重ね合わせ（Superposition）**：加算。
複数の概念ベクトルを一つのベクトルに重ねて保持する。
高次元の直交性により干渉が少なく、多くの概念を同時に保持できる。

### ベクトルの統合設計

ルーティング用の意味ベクトルと感覚データ・処理ロールを**単一のベクトルに統合する**方針を採用した。
別々に管理する案も検討したが、単一ベクトルのほうが管理が単純で、
データの種類の違いによって野相当のフィールドが自然に分かれることが期待できると判断した。

### EMAの罠（重要な警告）

blend（加重平均による取り込み）を繰り返すと、
記憶として保持したいパターンがすべて均等に混ざって識別不能になる（指数移動平均の収束）。

解決方針：
- 高温ノード（blend が活発な表面層）には長期記憶を担わせない
- 低温ノード（blend が緩やかな深部層）にバインド記憶を担わせる
- 温度差による層分離が EMA の罠の自然な解決策になりうる

### クリーンアップメモリ

VSA で Unbind した後のベクトルはノイズが乗っている。
既知のベクトルリストへの最近傍吸着（クリーンアップ）でシャープ化できる。
どのタイミングでどの程度クリーンアップを行うかはシステム安定性の重要課題。

### VSA次元数の検討

意味流通に加えて感覚データ・処理ロールも同じベクトルで担う場合は、現在の想定（256程度）から
10000 程度への引き上げが必要になると思われる。
多くの概念を重ね合わせても干渉を最小化するため。

---

## 記憶と学習の役割分離

```
意味    = VSAベクトル（何を表すか）
学習    = 経路の強化・淘汰（どこへ流れるか）
記憶    = 経路（ベクトルではなく通りやすい流路に記憶を持たせる）
非線形性 = 温度差（高温層と低温層の相互作用から生まれる）
```

Bind を万能記憶装置として使おうとすると EMA の罠に陥る。
「記憶はベクトルではなく経路に持たせる」という方針が現在の設計の核心。

短期記憶と長期記憶の分離は専用の記憶装置を追加しなくても、
場の温度差（表面高温・深部低温）から自然に生まれる可能性がある。

---

## 野

野はクラスタより大きな領域区分。現時点では設計者が容れ物を事前に用意することはしない。
野タグ（流体抵抗バイアス）は現在不採用。

ノードが持つデータの種類の自然な偏りによって、
クラスタのクラスタ（メタクラスタ）として野相当の構造が自発的に生まれることを期待する。

---

## IN / OUT

**IN**：外界からエネルギーと信号を注入するエネルギー源。
IN に近いノードほどエネルギーが豊富で高温になりやすく、発火・学習が活発になる。
ただし激しく信号が通り過ぎるため内部ベクトルが定着しにくい。

**OUT**：何を送ってもにじみでエネルギーを常時返送する（誘蛾灯）。
OUT のエネルギー返送を受けたノードはエネルギーが過剰になるため周囲に配り始め、
OUT 周辺のノードが自然に活発化する。
OUT の実行結果が次の IN として入ることで、外界とのフィードバックループが形成される。

---

## 意味処理ネットワークとしての可能性

伏流モデルは意味を運ぶ信号が経路を形成する「意味流通ネットワーク」として設計されているが、
意味処理ネットワークとしての機能も同じ場に統合できる可能性がある。

**データと意味を一つのベクトルに統合する**：
ルーティング用のベクトルと感覚データを別々に持つか一つにまとめるかを検討した結果、
単一のベクトルに統合する方針を採用した。データの種類の違いによって野相当のフィールドが自然に分かれることが期待できる。
これは初期頃の考えに立ち返ったものでもある。

**処理ロールをベクトル化する**：VSA 演算の処理ロール（バインド・アンバインド・クリーンアップなど）も
同じベクトル空間で表現し、ノードに割り当てることで、ノードのチェーンが処理パイプラインを形成できる可能性がある。

この方向に進む場合、VSA の次元数を大幅に引き上げる（10000 程度）必要がある。
ノードのチェーンによる VSA 操作の選択と実行が実際に機能するかどうかは未検証。
意味流通と意味処理が同じ場で共存するこの構想は、設計の根幹を拡張する可能性を持つ。

---

## 決まっていない重要な設計

以下は設計上の重要事項だが、どちらが正しいかは比較検証で確認が必要な段階にある。

### ノードの位置管理方式

**案A：座標レジスタを持つ**
各ノードが座標値を持ち、にじみは物理的な近傍全体に届く。
接続していない相手のにじみも受け取れる。

**案B：隣接リストのみ持つ**
ノードは接続先 ID のリストだけを持ち、物理座標は持たない。
にじみは接続先にのみ届く。移動＝接続先リストの更新として実装できる。
FPGA のメモリ効率が高い。

この選択によってノードの移動・探索の質が根本的に変わる。

### 信号のエネルギー減衰方式

**ホップ減衰方式**：信号自体が各ホップで弱まり、やがて消える。信号の生存時間が自然に制限される。

**ノード建て替え方式（ぼんやり信号回収）**：信号のエネルギーはホップごとに減衰せず、
代わりに通過したノードが自分のエネルギーを消費して信号を再エネルギー化して転送する。
液体ノードが信号のベクトルをほんのわずか吸い取りながら転送するという考え方もある。

### エネルギーの供給源

**IN のみ**：外界から IN を通じてのみエネルギーが注入される。

**場からも供給**：脳のグルコース供給のように、場全体からも一定量のエネルギーが各ノードに供給される。
この場合 IN は唯一の供給源でなくなる。

### 新生ノードの初期化と配置

**初期化**：周囲ノードのにじみから染まる方式か、完全ランダムから染まり始める方式か。

**配置**：活発なクラスタの近くに優先的に生まれるか（需要ベース）、場全体にランダムに生まれるか。

### エッジ（接続）の物理設計

ノード間の接続（エッジ）は完全な論理構造（隣接リストのみ）か、物理的な空間位置を持つかの中間的な設計が検討されているが、詳細は未確定。

### 信号衝突の処理方針

案B（並列スルー）と案C（側抑制）の選択が未確定（「信号」セクション参照）。

### ACK信号の伝播範囲

ACK が何ホップ遡るか、減衰しながら複数ホップ伝播するかが未確定。
1ホップのみだと直接接続先しか強化されない。
複数ホップだと強化が広がる代わりに「どの経路が本当に有効だったか」が曖昧になる。

### エネルギー散逸の設計

散逸が多すぎるとシステム全体の信号が消滅する。少なすぎるとエラー発火が連鎖する。
安定した平衡を保つための散逸率の設計が必要。

### エラー発火の採用可否

**採用する場合**：意味的に遠い信号でも強制的に処理・転送される。
新しい意味が取り込まれるきっかけになりうる。生物のひらめきに近い現象が起きる可能性がある。

**採用しない場合**：システムが安定する。強制発火による意図しない経路強化が起きない。

### ベクトル・信号・発火の詳細パラメータ

発火のエネルギー放出量・信号の交差率・resonance閾値・発火後の不応期の長さなど、
多くの数値的な判断が未解決のまま残っている。

---

## 設計の原則

**追加するより観察する**

記憶・感覚統合・内発的思考・連合野などの高次構造を固定機構として作るのではなく、
場・ノード・信号・エネルギー・温度・にじみという最小要素だけで自然発生するかどうかを先に確認する。

高次構造を追加しなければならなかった場合は「なぜ必要だったか」「何が自然発生しなかったか」を記録する。
これが最小必要構造の発見につながる。

**「液体状態（カオスの縁）」を維持する**

固体（過学習・硬直）でも気体（カオス・意味崩壊）でもない液体状態が、
学習と探索のバランスが取れた理想的な動作域となる。
快・不快・場の冷却・エネルギー経済がそれぞれ液体状態へ引き戻す力として機能する。

**局所判断の原則**

ノードは近傍の情報のみを参照し、大域的な状態（クラスタの形・全体のエネルギー分布・他ノードの座標など）を参照しない。
この制約がシステムの複雑化を防ぐと同時に、FPGAへの実装適性を保証する。
