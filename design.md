# 伏流モデル 設計

---

伏流モデルは、局所的な相互作用のみを持つノードが自律的に振る舞うことで、
設計者が明示的に構造を作らなくても知能的な処理が創発することを目指したネットワーク・フレームワーク。
「意味流通自己組織的ネットワーク」とも呼べる：信号が意味を運び、ノードは信号が持つベクトル（意味）を
覚えながら経路を形作る。

各ノードは自分の近傍にある情報（にじみ・到着信号・接続相手のエネルギー）だけを見て動作する。
全体の状態を知るノードは一つも存在しない。
それにもかかわらず、クラスタ形成・新陳代謝・クラスタ間接続・学習・記憶分離といった
高次構造が自然に創発することを設計の出発点とする。

設計の着想は脳・社会・経済・宇宙といった多様なスケールに共通する自己組織化の原理に基づいている。

---

## 層構造

システムは概念的に一つの物理層と二つの論理層に分けて理解できる。
ただしこれは概念的な分類であり、実装上はすべて同じノード・信号・にじみによって実現される。

| 層 | 名称 | 境界 | 役割 |
|---|---|---|---|
| 第一層 | **物理層**（意味整理層） | 場↔ノード間 | 物理的な網目構造の形成。資材置き場とライン（道）を作る |
| 第二層a | **論理層a**（意味流通層） | ノード↔ベクトル間 | 信号のベクトルでノード内ベクトルを編集する。純度を上げる。資材置き場の混ざりもの整理 |
| 第二層b | **論理層b**（意味処理層） | ベクトル↔信号間 | ノードのベクトルで信号が持つベクトルを編集・再構成する。バラバラな要素を関係性で意味にまとめる |

第二層aと第二層bは対称的な関係にある。

```
第二層a：信号 → ノード（外から中へ。ノードが染まる。学習・記憶）
第二層b：ノード → 信号（中から外へ。信号が解釈される。推論・理解）
```

この双方向性が「記憶」と「理解」を分けている。

**層間の関係**：
第二層aでベクトルが調整されると、第一層のクラスタが高純度になっていく。
第二層bで信号が再構成されると、より意味的に整理された信号が場を流れる。
三層は別々に動いているのではなく、互いに影響し合いながら全体を形作る。

### 現在の設計優先度

第一層は実現可能性が高く、先行研究（TNNなど）が動作を実証している。
第二層aは実現可能性が中程度。EMAの罠への対処が課題だが温度による層分離で解決の見通しがある。
第二層bは未踏領域に近い。「局所判断だけで意味構造化が創発するか」は実装して確認するしかない。

現時点では第一層と第二層aの相互作用が設計の中心。第二層bは将来の射程。

---

## 入力モジュール

### 感覚モジュール

外界からの情報を場に流す前に、VSAベクトルとして整理しやすい形に変換する専用モジュール。

```
外界の生データ
  ↓
感覚モジュール（専用処理）
  画像 → エッジ検出・パーツ分割・位置関係の符号化 → VSAベクトル
  音声 → 周波数分解・音素抽出 → VSAベクトル
  その他感覚 → 各種変換 → VSAベクトル
  ↓
場（伏流モデル本体）へ信号として投入
```

感覚モジュールは従来の信号処理・CNN等の既存技術をそのまま使える。
場の本体はVSAベクトルが来るという前提で設計できるのでシンプルになる。
感覚の種類が増えてもモジュールを追加するだけで場の設計を変えなくていい。

**感覚モジュールが担うこと**：
- パーツへの分解（「黒い鼻」「ブラウンの目」「白い耳」「長いマズル」の切り出し）
- パーツ間の相対位置関係の符号化（鼻を基準に目は右上3cm、など）
- VSAの束縛演算による関係の圧縮（`Bind(鼻, 目の方向)` など）

**場が担うこと**：
感覚モジュールから来たVSAベクトルを受け取り、流通・整理・処理する。
「パーツをどう切り出すか」という難問は感覚モジュール側に任せることで、
第二層bの実現可能性が大幅に上がる。

### 位置関係の符号化アプローチ

視覚入力でパーツの関係を把握するアプローチとして二案がある。

**案A：注視モデル（中心部のみ詳細）**

人間の視覚のように、一度に一点しか詳細に処理できない。
「黒い鼻に注目→次にブラウンの目に注目→次に白い耳に注目」と順番に走査する。
走査の軌跡と相対距離の記憶から全体像を構築する。
時系列的な処理で、ワーキングメモリ（定常波）に走査結果が蓄積される。
「今どこに注視しているか」という大域的な状態管理が必要になる可能性がある。

**案B：相補的位置関係（推奨）**

各パーツが互いに「私は〇〇の隣にいる」という関係を持ち合う。
中心点を明示的に決めず、パーツ同士の相対関係の網目から全体構造が浮かび上がる。

```
鼻↔目：距離・方向
目↔耳：距離・方向
耳↔マズル：距離・方向
→ 関係の網目全体が「犬の顔」を表現する
```

VSAの束縛演算と相性が良く、完全に分散的で局所判断の原則に合致する。
各ノードが隣接パーツとの関係だけを持てばよく、大域的な状態管理が不要。

### 言語モジュールとLLMとの接続

感覚モジュールの代わりに言語ベクトル化モジュールを繋ぐと、LLMと相互にやり取り可能になる。

```
LLM
  ↓↑
言語ベクトル化モジュール
（テキスト → VSAベクトル / VSAベクトル → テキスト）
  ↓↑
場（伏流モデル本体）
```

**LLMと伏流モデルの役割分担**：

| | LLM | 伏流モデル |
|---|---|---|
| 得意なこと | 言語の統計的パターン・表層的な意味関係 | 意味の物理的な位置関係・経路としての記憶・長期的な構造の安定 |
| 比喩 | 作業台（一度限りの文脈処理） | 倉庫と棚（長期的な構造の保持・更新） |

LLMが言語化できない・苦手な部分を伏流モデルが補える可能性がある。

**処理の流れの例**：
```
LLM「犬がりんごを咥えている」
  ↓ 言語ベクトル化モジュール
{犬, りんご, 口, 咥える} の関係をVSAに変換
  ↓ 場に信号として流入
第一層：「犬」「りんご」「口」に対応するクラスタが物理空間で接続
第二層a：各クラスタのノードベクトルが純化・定着
第二層b：関係性が整理されて構造化された意味として保持
  ↓ 言語ベクトル化モジュール
テキストに戻してLLMに返す
```

LLMが一度限りの文脈で処理していたものを、伏流モデルが長期的な構造として保持・更新し続ける。

---

## 第二層bの処理例：「犬がりんごを口に咥える」

第二層bで何が起きるかを具体的に説明する。

**入力**：
感覚モジュールまたは言語モジュールから
`{犬, りんご, 口, 咥える}` という要素群が信号として場に流入する。

**第一層での位置決め**：
各要素に対応するクラスタが物理空間に存在し、道でつながっている。
「犬クラスタ」「りんごクラスタ」「口クラスタ」「動作クラスタ」など。

**第二層bでの再構成**：

```
ステップ1：要素の抽出
  「犬」= ブラウンの毛・黒い鼻・白い耳・長いマズル・...
  「りんご」= 赤・丸・白っぽい点々・一部が黄色・...
  「口」= 開いた状態・犬の口

ステップ2：中心認識ポイントの確立
  「黒い鼻」を基準点として選択
  他の要素をこの基準との相対位置で関連付ける

ステップ3：関係性の整理
  口は犬の顔の一部 → 口と犬のBinding
  りんごは口のところにある → りんごと口の位置関係のBinding
  咥えるは口とりんごの動作関係 → 動作のBinding

ステップ4：統合
  Binding群をSuperpositionで重ね合わせ
  「犬がりんごを口に咥える」という一つの構造化ベクトルに圧縮
```

**ポイント**：
各ステップは局所的なノードの判断（類似するにじみへの反応・信号の通過・VSA演算）から生まれる。
「犬がりんごを咥えている」という全体像を把握しているノードは一つも存在しない。
それでも全体として構造化された意味が出力される。



## 基本要素

システムは以下の最小要素で構成される。
クラスタ・大ノード・野などの上位構造はこれらの相互作用から創発するものであり、
設計者が手作りするものではない。

| 要素 | 説明 |
|---|---|
| **ノード** | 処理単位。ベクトル・エネルギー・温度を持つ |
| **信号** | ノード間を伝播するパケット。ベクトルとエネルギーを持つ |
| **エネルギー** | 信号の強さであり、ノードの生存燃料。信号転送量によって供給される |
| **温度** | ノードの局所的な活性状態。場が積極的に冷やそうとする |
| **にじみ** | ノードが周囲に常時放つ微弱な自己告知。光のように拡散して減衰する |
| **場** | ノードが存在する有向グラフ空間。物理法則として斥力・引力・冷却が働く |
| **クラスタ** | 意味的に近いノードが自然に集まった集合（外部観測概念。ノード自身は認識しない） |
| **大ノード** | クラスタが一つのノードとして振る舞う概念。再帰的な階層構造を形成する |
| **IN / OUT** | 外界との接続端末 |

---

## 二つの空間

伏流モデルには二つの異なる空間が存在する。

### 有向グラフ空間（意味整理層の舞台）

ノードが実際に存在し、移動し、結合する物理的な空間。
ノード同士の距離・密度・引力・斥力はこの空間で定義される。
ノードの座標管理はCPU/GPUが担う。

### ベクトル空間（意味の舞台）

VSA（Vector Symbolic Architecture）の高次元超密度ベクトルが存在する抽象空間。
ノードが持つベクトルはこの空間から来る。
有向グラフ空間での位置とは独立している。

**二空間の橋渡し**：
ベクトル空間での類似度（コサイン類似度）が、有向グラフ空間での引力・斥力に変換される。
意味が近いノードは物理的に引き寄せられ、意味が遠いノードは押し退け合う。
この写像がクラスタ形成の根本原理。

---

## 意味整理層（第一層）

意味整理層はノードの位置関係を通じて、意味の近いノードが自然に集まるクラスタを形成する。

### 場の物理法則

場には以下の物理法則が常時働いている。
ノードがどのような状態であっても（活発・凍結問わず）場の法則は作用する。

**斥力**：
近づきすぎたノード同士を押し返す。物理的存在としての反発。

**冷却**：
場はノードを積極的に冷やそうとする。局所密度が高いほど冷却が速い（ラジエータに冷却剤が流れるイメージ）。
密度が高い場所ほど冷却剤がたくさん流れるため、クラスタ中心部ほど冷えやすい。
これはチューリングパターン（活性化因子と抑制因子の拡散速度差）と同じ原理であり、
クラスタサイズが自然に上限を持つ原因になる。

### ノードの活発時の動き（意味整理層）

活発なノードは以下の力で動く。

**引力（エネルギー依存）**：
活発ノードは周囲のにじみを感知し、自分と類似度が高いにじみがある方向に引き寄せられる。
エネルギーが高いほど引力が強くなる。
これによりクラスタ中心（高エネルギー）ほど近い意味のみに強く引き合い、
外縁部（低エネルギー）は遠い意味にも弱く引かれる（橋渡し役になれる）。

**斥力強化（エネルギー依存）**：
エネルギーが高いほど遠い意味への斥力が強くなる。
クラスタが異質なノードを自然に排除し、高純度に保たれる。

```
引力の強さ = 類似度^感度 × エネルギー^指数 / 距離
斥力強化   = 基本斥力 × (1 + 遠さ × エネルギー × スケール)
```

### クラスタ形成のメカニズム

クラスタはノードが「クラスタを作ろう」と意図するのではなく、
にじみの勾配を辿った結果として自然に形成される。

```
初期：ノードがランダムに分布。にじみが弱く引力も弱い。
中期：意味の近いノードが少しずつ集まり始める。
     集まるほどにじみが重なり合い、引力が強まる正のフィードバックが働く。
完成：クラスタ内は高密度・高エネルギー・強引力。
     外縁部は低エネルギーで隣クラスタとも弱く引き合う（橋渡し）。
```

### 大ノードと再帰的階層構造

クラスタは外部から見ると一つの「大ノード」として振る舞う。
大ノードは個別ノードと同じ原理で互いに引き合い、より大きな構造を形成する。
大ノードの結合は外縁部ノードを通じて行われる。

ノードが持つベクトルには内部に仮想の網目構造が含まれていると考えられる。
（VSAの束縛演算によって関係性のネットワークが一本のベクトルに圧縮されているため。）
この性質により、ノードのスケール・クラスタのスケール・大ノードのスケールが
数学的に自己相似（フラクタル）になる。

### 凍結（意味的休眠）

凍結はノードが「意味的に休眠している状態」を指す。処理コスト削減が目的ではない。

温度が低下して閾値を下回ると凍結に入る。

**凍結ノードがしないこと**：にじみ放出・信号送出・ベクトル学習
→ 意味を発していない、意味に反応していない状態。

**凍結ノードがすること**：物理移動（斥力・慣性）
→ 物理的には存在し続け、場の法則によって漂い続ける。

凍結ノードはにじまないため、他のノードから引力の源として「見えない」。
漂いながら偶然に活発ノードの信号が届く距離に入ったとき復帰できる。

**凍結と温度・エネルギーの関係**：

温度とエネルギーは独立した概念として分離する。

```
温度（temp）：物理層の状態。凍結・解凍の判定に使う。
              密度冷却と信号加熱の収支で変化する。
              freeze_tempを下回ると凍結。thaw_tempを超えると解凍。
              二つの閾値の差がヒステリシス（チャタリング防止）として機能する。

エネルギー（energy）：意味流通層の消耗度。生存・死亡の判定に使う。
                     活発中ににじみ・信号送出のたびに消費。
                     信号が通過するたびに回復（後述）。
                     凍結中は消費しない。
```

**ノードの状態遷移**：

```
[活発]
  ├─ にじみ・信号送出でエネルギー消費
  ├─ 信号通過でエネルギー回復・温度上昇
  ├─ 局所密度×冷却係数で温度低下
  ↓ 温度が freeze_temp を下回る
[凍結]
  ├─ にじみ・信号停止（エネルギー消費なし）
  ├─ 物理移動継続（斥力・慣性）
  ├─ 密度冷却は継続
  ├─ 信号が届くと温度上昇・エネルギー回復
  ↓ 温度が thaw_temp を超える
[活発]に復帰

[死]（オプション）：
  energy <= 0 で消滅。
  クラスタ内凍結ノード：エネルギー消費なし → 死なない
  迷子ノード：解凍→浪費→凍結を繰り返し消耗 → 死ぬ
```

### 紐化問題と自動解消

**問題**：
ベクトルが連続的に分布している場合、類似度による引力は必ず紐状・環状の構造を生む。
これは1次元的な類似度空間を2次元物理空間に写像するときの構造的な帰結。

**自動解消のメカニズム**：
意味流通層によるベクトルブレンドが進むと、連続体だった色相分布が離散的なクラスタに分化する。
連続体が離散化されると、紐の途中に「意味的断絶」が生まれ引力が切れてちぎれる。
高次元VSAベクトルを使う本番実装では、多次元的な類似度により紐化がそもそも起きにくい。

**クラスタサイズの自然な上限**：
密度冷却モデルにより、クラスタが大きくなるほど中心部が冷えて凍結し始める。
凍結ノードはにじまないため引力の源にならなくなる。
これが自然なサイズ上限として機能する。チューリングパターンと同じ原理。

---

## にじみ

ノードは常時、自分のベクトルとエネルギーを近傍に微弱に漏らす（にじみ）。

にじみは光のように拡散して減衰する。
にじみはチャンネル（どの意味帯か）と流れの向きを持つ。
ノードは類似するチャンネルほど強く引き寄せられ、類似しないチャンネルほど強く押しのけられる。

にじみの役割は二つある。

| 役割 | 内容 |
|---|---|
| **存在告知** | 近傍に自分の波長を知らせ、他ノードの移動・接続判断の材料になる |
| **報酬の帰還** | 発火後ににじみが送信元ノードへ逆流し、有効だった経路が強化される |

### 集団にじみ

個別ノードのにじみに加えて、密度が高い領域はまとまりとしてより広い範囲ににじみを放出する。

これは個々のノードがにじむのではなく、密度という場の性質によって生まれる現象。
クラスタを明示的に認識する必要はなく、局所密度が閾値を超えた領域から自然に生じる。
これによって遠くの孤立ノードがクラスタの存在を感知できる。

将来的には個々のノードのにじみだけで実装されるべきだが、
デモや初期実装では集団にじみを補助的に追加してよい。

---

## 意味流通層（第二層）

意味流通層は信号を目的地へ案内し、信号の通過によってノード内ベクトルを調整する。

### エネルギーの源泉

「信号の転送量がエネルギー供給量を決める」という設計を採用する。

これは神経血管カップリングと同じ原理：
神経活動が高い領域には血流が増加しグルコース・酸素が優先的に供給される。
電気的な活動（信号転送）が燃料（エネルギー）の流入を引き起こす。

血管系でも同様：
使われる経路ほど血流が増えて血管が太くなり、使われない経路は細くなって消える。

```
信号が通過する → エネルギーが流れ込む
転送量が多いノード（クラスタ中心）→ エネルギーが豊富 → 引力が強くなる
転送量が少ないノード（外縁・迷子）→ エネルギーが枯渇 → 凍結・死へ向かう
```

これによりクラスタ中心は自然にエネルギーが豊富になり、
「クラスタ中心はどこからエネルギーを得るか」という問題が解決される。

### 信号

信号はベクトルとエネルギーを持つ。エネルギーとベクトルは一体として動く。
信号は液体のような性質を持ち、場を流れるイメージに近い。

複数の信号が同時に同じノードに届いた場合は重ね合わせ（Superposition）で合成される。

### 信号交差（通過ノードとの相互影響）

信号がノードを通過するとき、信号とノードは互いに少しずつ影響し合う。

```
新しい信号ベクトル   = 信号 × (1-r_signal)  + ノードベクトル × r_signal
新しいノードベクトル = ノード × (1-r_node)   + 信号ベクトル  × r_node
```

r_signal と r_node は別々に設定できる（非対称交差）。
信号エネルギーが強いほど交差の割合が大きくなる方向が有力。

この仕組みにより、たらい回しにされ続ける信号は徐々に「ぼんやりした意味」になり、
最終的にどこかのノードと共鳴して吸収される（「永遠に処理されない信号」問題の自然な解消）。

意味流通層でのベクトル調整が蓄積すると、意味整理層のクラスタが高純度になっていく。
つまり連続的に分布していたベクトルが徐々に離散的なクラスタに分化する。
これが紐化の自動解消とクラスタの確立につながる。

### 接続の方向

ノードは自分よりエネルギーを持つノード（より富めるもの）に接続しようとする。
エネルギーが多すぎるノード（クラスタ中心部・IN/OUT 周辺など）は自分より弱いノードに接続しようとする。
接続は有向グラフを形成する。信号は一方向に流れる。双方向になるのは両側から手を伸ばしたとき。

### 発火

**通常発火**：保有ベクトルと到着信号ベクトルの類似度（resonance）が閾値を超えたとき。
エネルギーを放出しながら信号を次に送る。温度が上昇する。発火するほど寿命が伸びる。

**エラー発火（強制発火）**：残エネルギーが上限を超えたとき。
内側からの圧力でゲートが破れる受動的放出。送り先を選ばない。
新しい意味の偶発的な取り込みや連想の広がりを生む可能性がある。

### ベクトルの取り込み（blend）

信号が届くたびに、残エネルギーに応じた割合 r でベクトルを取り込む。

- 飢えているほど（残エネルギー少）→ r 大：外部ベクトルに染まりやすい
- 満たされているほど（残エネルギー多）→ r 小：自分のベクトルを保つ

ACK信号が多く返ってきているノードは「今の自分でうまくいっている」ため染まりにくくなる。

### ACK信号（逆流確認応答）

下流ノードあるいはOUTが発火・出力を達成したとき、その成功信号が上流に向かって逆流する。
正方向とは逆方向に流れる別チャネルとして存在する。

| | にじみ帰還 | ACK信号 |
|---|---|---|
| 発生源 | 発火したノードから | 下流ノード・OUTから |
| 強さ | 弱い（にじみ） | 強い（明示的な信号） |
| 対象 | 近傍全体・減衰して届く | 上流の特定経路 |
| 役割 | 軽い報酬信号 | ルーティング方向の強化 |

ACK信号の強さが上流ノードのルーティング判断に影響する。
局所的なACK強度の比較だけでグローバルな目標を知らずにルーティングが最適化される。

### 定常波とリフレイン

クラスタ内でノードが互いに発火し合い、信号が経路を循環し続けると「定常波」が生まれる。
これが「今考えていること」の物理的な実体になりうる（ワーキングメモリに相当）。

定常波は：
- ループする信号がホップ減衰で自然に消えるため、外部からの補充がないと消える
- 外部からの新しい信号が来ると既存の定常波と相互作用して更新される
- 定常波の強さが閾値を超えたとき初めてOUTやハブへ送られる

---

## ノード詳細

### 保有情報

| 情報 | 説明 |
|---|---|
| VSAベクトル | 意味内容・感覚データ・処理ロールを統合した単一の高次元超密度ベクトル |
| エネルギー | 生存燃料。信号通過で増え、にじみ・発火で減る |
| 温度（T_local） | 信号の通過で上昇。場が冷やそうとする |

### 物質の三態

ノードは温度の高さによって振る舞いが変わる。

| 状態 | 温度 | 振る舞い |
|---|---|---|
| **固体**（低温） | 冷えて凍結 | 意味的休眠。にじみのみ放射。物理移動は継続 |
| **液体**（適温） | 程々 | 転送のみ担う。多少ベクトルを吸い取りながら次に送る |
| **気体**（高温） | 熱くなる | 活発にベクトルを取り込み、発火・学習する |

場は積極的に冷やそうとするため、ノードは自然に固体方向へ向かう。
信号の通過が続くときだけ温度が上昇して気体・液体状態を維持できる。

### 経路の段階的固定化

初期状態ではベクトルが弱く共鳴度が低い。
そのため最初の信号は広く薄く全方向に探索する（蛮行）。
繰り返し同じ信号が通ることでベクトルが育ち、共鳴度が上がり、特定の経路が強化される。

### ライフサイクル

自律的な三役職遷移（外部から管理しない）：

1. **白紙の若手**（外縁・高温）：多様な信号を浴びてベクトルが変化しやすい
2. **特化した職人**（コア・低温）：特定の信号に高く共鳴する。安定して長寿
3. **泥水の経路**（外周・適温）：多様な信号を通した結果、どの信号ともそこそこ共鳴する液体ノード。クラスタ間橋渡しを担う

---

## ノードの動機とエネルギー経済

エネルギーは栄養でもあり毒でもある。この対称的な制約がノードの能動的な振る舞いを生む。

- 発火しないと枯渇して死ぬ → 信号を引き寄せようとする
- エネルギーを持ちすぎると死が近づく → 周囲に送り出そうとする
- 発火するほど寿命が伸びる → 信号の流れの中に自分を置こうとする

### ノードのエネルギー収支

| 収入 | 支出 |
|---|---|
| 信号の通過（転送量に比例） | 代謝コスト（存在するだけ） |
| ACK信号の帰還 | にじみコスト（告知コスト） |
| にじみ帰還（発火時） | 発火時手数料（激しい消費） |
| | 発火しない信号の転送コスト |

### 発火しない信号の転送コスト

類似度が閾値未達の信号を転送するとき、ノードは自分のエネルギーを消費して信号を通す。
役に立たない信号が多いノードはエネルギーが枯渇してクラスタから押し出される。
この仕組みにより、意味的に適切な経路が自然に形成され、不要な経路は淘汰される。

---

## クラスタ

クラスタはノードが内部的に認識する構造ではない。
ノードは「近傍のにじみと信号を見て判断する」だけであり、
自分がどのクラスタに属するかを知らない。
クラスタは外から観測したときに「意味的に近いノードが集まっている」と見えるパターンにすぎない。

### クラスタ間の接続

クラスタが成長するにつれてクラスタ同士の間に接続が生まれる。

**押し出し型**：
クラスタ中心で濁ったノードが外縁部に積もり始める。
そのノードは元クラスタ周辺の信号には馴染みがあるが、完全には染まりきっていない。
周囲のクラスタで同様に外縁部に積もったノードと徐々に近づいて接続し、橋を形成する。

**引き寄せ型**：
クラスタ外周部のノードはそのクラスタに染まりきっていないため、周囲のクラスタにも引き寄せられやすい。
周囲のクラスタの外周部も同様であるため、徐々に相互接続が始まる。

どちらの場合も、橋渡しノード（ブリッジ）の共通した特徴：
- どのクラスタとも完全には一致しない「泥水化したベクトル」を持つ
- 信号が通り続ける限り寿命が延びる
- 信号が途絶えると速やかに死ぬ（経路の自動剪定）

### クラスタの圧と引力（外部観測値）

```
クラスタの圧          = Σ残エネルギー / ノード数
クラスタの放射ベクトル = Σ(各ノードのベクトル × 残エネルギー) / Σ残エネルギー
```

これはデバッグ用の外部観測値。ノードはこれを直接参照しない。

---

## エネルギー（システム全体）

### システム全体のエネルギー収支

外部からエネルギーが入る口として IN が確定している。
信号の転送量によってもエネルギーが供給される（神経血管カップリングのアナロジー）。

エネルギーが出ていく出口：
- OUT が受け取ったエネルギーの一部を系外へ放出する
- 発火・転送の手数料として消費されたエネルギーが「熱」として散逸する（確定）
- 系のどこかに「仮想捨て先（虚空）」がある（エラー発火が行き場を失ったとき専用）

---

## 快・不快と温度管理

外界からの快・不快信号は、ネットワーク全体の温度管理として機能する。

**快＝冷やす**：くっつきやすさを上げて現在のクラスタ構造を安定させる。
成功した経路・パターンを凝固させて定着させる仕組み。

**不快＝温める**：ランダムなノイズを注入し、くっつきやすさを下げてノードを漂流させる。
硬直状態から脱出し、新しい可能性を能動的に探索させる仕組み。

この二つが組み合わさることで、「学習と探索のバランス」が温度という単一の軸で制御される。

---

## IN / OUT

**IN**：外界からエネルギーと信号を注入するエネルギー源。
IN に近いノードほどエネルギーが豊富で高温になりやすく、発火・学習が活発になる。
ただし激しく信号が通り過ぎるため内部ベクトルが定着しにくい。

**OUT**：何を送ってもにじみでエネルギーを常時返送する（誘蛾灯）。
OUT のエネルギー返送を受けたノードはエネルギーが過剰になるため周囲に配り始め、
OUT 周辺のノードが自然に活発化する。
OUT の実行結果が次の IN として入ることで、外界とのフィードバックループが形成される。

---

## VSA（Vector Symbolic Architecture）

ベクトルは VSA の高次元超密度ベクトル（HDV）を用いる。高次元であるため、ランダムなベクトル同士はほぼ直交する。

主要演算は二つ。

**結合（Binding）**：XOR 演算。
「A と B を結び付けた概念」を生成する。結果は A にも B にも似ておらず、直交に近い新しいベクトルになる。
キー（A または B）があれば元の概念を取り出せる（Unbinding）。

**重ね合わせ（Superposition）**：加算。
複数の概念ベクトルを一つのベクトルに重ねて保持する。
高次元の直交性により干渉が少なく、多くの概念を同時に保持できる。

ノードが持つベクトルには内部に仮想の網目構造が含まれている。
VSAの束縛演算によって関係性のネットワークが一本のベクトルに圧縮されているためであり、
これがノード・クラスタ・大ノードのスケールを跨いだ自己相似性の数学的な根拠となる。

### ベクトルの統合設計

ルーティング用の意味ベクトルと感覚データ・処理ロールを**単一のベクトルに統合する**方針を採用した。
データの種類の違いによって野相当のフィールドが自然に分かれることが期待できる。

### EMAの罠（重要な警告）

blend（加重平均による取り込み）を繰り返すと、
記憶として保持したいパターンがすべて均等に混ざって識別不能になる（指数移動平均の収束）。

解決方針：
- 高温ノード（blend が活発な表面層）には長期記憶を担わせない
- 低温ノード（blend が緩やかな深部層）にバインド記憶を担わせる
- 温度差による層分離が EMA の罠の自然な解決策になりうる

### クリーンアップメモリ

VSA で Unbind した後のベクトルはノイズが乗っている。
既知のベクトルリストへの最近傍吸着（クリーンアップ）でシャープ化できる。

### VSA次元数の検討

意味流通に加えて感覚データ・処理ロールも同じベクトルで担う場合は、
10000 程度への引き上げが必要になると思われる。

---

## 記憶と学習の役割分離

```
意味    = VSAベクトル（何を表すか）
学習    = 経路の強化・淘汰（どこへ流れるか）
記憶    = 経路（ベクトルではなく通りやすい流路に記憶を持たせる）
非線形性 = 温度差（高温層と低温層の相互作用から生まれる）
```

「記憶はベクトルではなく経路に持たせる」という方針が現在の設計の核心。

短期記憶と長期記憶の分離は専用の記憶装置を追加しなくても、
場の温度差（表面高温・深部低温）から自然に生まれる可能性がある。

---

## 意味処理層（第三層）

信号内のベクトルを演算して新たな意味を生成する層。
現時点では設計の射程外。意味整理層と意味流通層が安定して機能することを確認した後に設計を進める。

---

## 処理装置の役割分担

| 処理装置 | 担当 |
|---|---|
| FPGA | ベクトル演算（類似度計算・束縛・重ね合わせ・信号転送・学習） |
| CPU/GPU | 座標管理・斥力・引力方向計算・物理移動 |
| にじみ | 座標参照（CPU/GPU）＋ベクトル内容（FPGA）の混合・担当未確定 |
| NPU | 将来的な上位レイヤー判断（意味処理層）・現時点では未実装 |

---

## 設計の原則

**追加するより観察する**

記憶・感覚統合・内発的思考・連合野などの高次構造を固定機構として作るのではなく、
場・ノード・信号・エネルギー・温度・にじみという最小要素だけで自然発生するかどうかを先に確認する。

高次構造を追加しなければならなかった場合は「なぜ必要だったか」を記録する。

**「液体状態（カオスの縁）」を維持する**

固体（過学習・硬直）でも気体（カオス・意味崩壊）でもない液体状態が、
学習と探索のバランスが取れた理想的な動作域となる。

**局所判断の原則**

ノードは近傍の情報のみを参照し、大域的な状態を参照しない。
この制約がシステムの複雑化を防ぐと同時に、FPGAへの実装適性を保証する。

---

## 決まっていない重要な設計

### ノードの位置管理方式

**案A：座標レジスタを持つ**
各ノードが座標値を持ち、にじみは物理的な近傍全体に届く。接続していない相手のにじみも受け取れる。

**案B：隣接リストのみ持つ**
ノードは接続先 ID のリストだけを持ち、物理座標は持たない。
にじみは接続先にのみ届く。FPGA のメモリ効率が高い。

この選択によってノードの移動・探索の質が根本的に変わる。

### 信号のエネルギー減衰方式

**ホップ減衰方式**：信号自体が各ホップで弱まり、やがて消える。

**ノード建て替え方式**：信号のエネルギーはホップごとに減衰せず、
通過したノードが自分のエネルギーを消費して信号を再エネルギー化して転送する。

### 信号衝突の処理方針

**案B：並列スルー**（当面の実装方針）
衝突を明示的に処理せず、Superpositionで重ね合わせて並列に流す。

**案C：側抑制による分離**（将来的な理想形）
同じ意味の信号が衝突したときは側抑制で1本に絞り込む。
異なる意味の信号が衝突したときは共存させる。

### 新生ノードの初期化と配置

**初期化**：周囲ノードのにじみから染まる方式か、完全ランダムから染まり始める方式か。

**配置**：活発なクラスタの近くに優先的に生まれるか（需要ベース）、場全体にランダムに生まれるか。

### ACK信号の伝播範囲

ACKが何ホップ遡るか、減衰しながら複数ホップ伝播するかが未確定。

### エネルギー散逸の設計

散逸が多すぎるとシステム全体の信号が消滅する。少なすぎるとエラー発火が連鎖する。
安定した平衡を保つための散逸率の設計が必要。

### エラー発火の採用可否

**採用する場合**：新しい意味が取り込まれるきっかけになりうる。生物のひらめきに近い現象が起きる可能性がある。

**採用しない場合**：システムが安定する。強制発火による意図しない経路強化が起きない。
